<%- include('../partials/header.ejs') %>
<main>
    <h1>Service Requests</h1>

    <% requests.forEach(request => { %>
        <div class="service-request-row">
            <div class="service-request-info">
                <p><strong><%= request.customerName %></strong> — <%= request.vehicle_info %> — <%= request.service_type %></p>
                <% if (request.notes) { %><p class="customer-notes">Customer notes: <%= request.notes %></p><% } %>
                <span class="status-badge status-<%= request.status.toLowerCase().replace(' ', '-') %>"><%= request.status %></span>
            </div>

            <form method="POST" action="/employee/service-requests/<%= request.id %>" class="service-request-form">
                <select name="status">
                    <option value="Submitted" <%= request.status === 'Submitted' ? 'selected' : '' %>>Submitted</option>
                    <option value="In Progress" <%= request.status === 'In Progress' ? 'selected' : '' %>>In Progress</option>
                    <option value="Completed" <%= request.status === 'Completed' ? 'selected' : '' %>>Completed</option>
                </select>
                <textarea name="employeeNotes" placeholder="Employee notes"><%= request.employee_notes || '' %></textarea>
                <button type="submit">Update</button>
            </form>
        </div>
    <% }) %>
</main>
<%- include('../partials/footer.ejs') %>